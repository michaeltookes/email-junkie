name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test-lint:
    name: Build, Test & Lint
    runs-on: macos-15
    timeout-minutes: 20
    defaults:
      run:
        working-directory: EmailJunkie
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show toolchain
        run: |
          xcodebuild -version
          swift --version

      - name: SwiftLint
        run: |
          if ! command -v swiftlint >/dev/null 2>&1; then
            brew install swiftlint
          fi
          swiftlint --version
          swiftlint lint --strict

      - name: Build & Test
        run: |
          set -o pipefail
          xcodebuild test \
            -project EmailJunkie.xcodeproj \
            -scheme EmailJunkie \
            -destination 'platform=macOS' \
            CODE_SIGNING_ALLOWED=NO
